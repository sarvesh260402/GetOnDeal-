/**
 * Optional runtime frontend env injection.
 * Copy and adapt this for hosting environments that can emit JS config.
 */
window.__GOD_ENV__ = {
  API_BASE_URL: 'http://localhost:5000/api'
  // Production example: 'https://api.getondeal.com/api'
};
